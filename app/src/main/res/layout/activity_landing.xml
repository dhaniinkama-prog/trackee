<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_default">

    <ImageView
        android:id="@+id/landingMascot"
        android:layout_width="251dp"
        android:layout_height="208dp"
        android:layout_marginTop="124dp"
        android:adjustViewBounds="true"
        android:src="@drawable/landing_character"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/buttonContinue"
        android:layout_width="303dp"
        android:layout_height="44dp"
        android:layout_marginTop="24dp"
        android:backgroundTint="#C2C9FF"
        android:text="Continue"
        android:textAllCaps="false"
        android:textStyle="bold"
        app:cornerRadius="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/landingDescription"
        app:layout_constraintVertical_bias="0.0" />

    <TextView
        android:id="@+id/landingTitle"
        android:layout_width="353dp"
        android:layout_height="83dp"
        android:layout_marginTop="32dp"
        android:paddingHorizontal="32dp"
        android:text="Your cheerful\nhabit friend"
        android:textAlignment="viewStart"
        android:textColor="#000000"
        android:textSize="34sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/landingMascot" />

    <TextView
        android:id="@+id/landingDescription"
        android:layout_width="354dp"
        android:layout_height="62dp"
        android:layout_marginTop="12dp"
        android:lineSpacingExtra="4dp"
        android:paddingHorizontal="32dp"
        android:text="Build good habits, stay kind to yourself, and enjoy every little progress — one smile at a time."
        android:textAlignment="viewStart"
        android:textColor="#444444"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.508"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/landingTitle" />

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="367dp"
        android:layout_height="185dp"
        android:layout_marginTop="20dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.931"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/buttonContinue"
        app:srcCompat="@drawable/lingkaran_landing" />

</androidx.constraintlayout.widget.ConstraintLayout>
